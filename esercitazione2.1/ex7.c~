#include <stdio.h>
#include <stdlib.h>
int main(){
int i,n,*p=(int*) malloc (sizeof(int)),*minimo=(int*) malloc (sizeof(int));
p=&i;
minimo=&n;
printf("insersci numero intero positivo i e n :\n");
scanf("%d",p);
n=*p;
while (*p!=0){
	if (*p<*minimo){
		*minimo=*p;
		}
	printf("insersci numero intero positivo i :\n");
	scanf("%d",p);
}

printf("il valore minimo è: %d\n",*minimo);
int free(int* p);
int free(int* minimo);
}
